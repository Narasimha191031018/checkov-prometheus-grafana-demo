trigger:
- none

pool:
  vmImage: 'ubuntu-latest'

steps:
# Step 0: Install Terraform
- task: ms-devlabs.custom-terraform-tasks.custom-terraform-installer-task.TerraformInstaller@0
  inputs:
    terraformVersion: '1.15.7'

# Step 1: Terraform Init (always reconfigure backend)
- task: AzureCLI@2
  displayName: 'Terraform Init'
  inputs:
    azureSubscription: 'ado-terraform-sp'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      cd terraform
      terraform init -reconfigure

# Step 2: Terraform Import RG (one-time step)
# Run this once to bring rg-checkov-demo into backend state, then you can comment/remove it
- task: AzureCLI@2
  displayName: 'Terraform Import Resource Group'
  inputs:
    azureSubscription: 'ado-terraform-sp'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      cd terraform
      terraform import azurerm_resource_group.demo /subscriptions/6cd70fb3-2991-4056-afe9-60659e59898a/resourceGroups/rg-checkov-demo

# Step 3: Terraform Plan
- task: AzureCLI@2
  displayName: 'Terraform Plan'
  inputs:
    azureSubscription: 'ado-terraform-sp'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      cd terraform
      rm -rf .terraform terraform.tfstate*
      terraform init -reconfigure
      terraform plan -out=tfplan

# Step 4: Publish Terraform Config + Plan (exclude .terraform/)
- task: PublishPipelineArtifact@1
  displayName: 'Publish Terraform Config + Plan'
  inputs:
    targetPath: terraform
    artifact: terraform
    publishLocation: 'pipeline'

# Step 5: Run Checkov (non-blocking, capture results)
- script: |
    docker run --rm -v $(pwd)/terraform:/tf \
      bridgecrew/checkov -d /tf -o json > checkov_results.json || true
  displayName: 'Run Checkov Scan (Docker - Capture Results)'

# Step 6: Publish Checkov Results Artifact
- task: PublishPipelineArtifact@1
  displayName: 'Publish Checkov Results'
  inputs:
    targetPath: checkov_results.json
    artifact: checkov_results
    publishLocation: 'pipeline'

# Step 7: Setup Python Virtualenv + Start Exporter
- script: |
    python3 -m venv venv
    source venv/bin/activate
    pip install --upgrade pip setuptools wheel flask
    python exporter/exporter.py checkov_results.json &
  displayName: 'Start Exporter Service (Python)'
